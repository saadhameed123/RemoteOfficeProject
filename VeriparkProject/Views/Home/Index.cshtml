@model IEnumerable<Coreplus.Sample.Api.Types.Practitioner>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.level)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.id)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.level)
                </td>

                <td>
                    @Html.ActionLink("Report", "EmployeeReportIndex", "Home", new {PractionerID=item.id})
                    @*@Html.ActionLink("Generate Report", "Report", new { /* id=item.PrimaryKey */ }) |*@

                </td>
            </tr>
        }
    </tbody>
</table>


<script>

</script>